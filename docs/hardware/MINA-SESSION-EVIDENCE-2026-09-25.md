# Mina (char 2, node 192.168.8.140) — session evidence, 2026-09-25 (UTC 21:30–22:05)

## Operator statement (verbatim intent)
Wiring confirmed on ALL parts: Eye = PCA9685 ch3, Neck = ch7, Jaw = ch11. LED lights wired to power and ground on the PCA9685 (no PWM dimming, on/off only). Power to the PCA9685 confirmed; each servo tested individually; the MDD10A opens and closes the linear-actuator door. Rose lamp, AI, speaker and microphone work.

## State found on the node (before this session touched anything)
- parts.json already carried jaw ch11 / neck ch7 / eye ch3 (edited via the part editor 20:17–20:19 UTC today; `updated` stamps). Part 10 (eye LED/laser) had `pin: 15` typed into the pin field but `config.channel` still 0 — the PCA9685 light path reads config.channel, so the LED would have been driven on ch0.
- Jaw calibration 2:1 (28–84°, measured) was DELETED at 20:19:45 UTC via the single-part Clear endpoint (`/var/log/monsterbox.err`: "Calibration profile DELETED for 2:1 (was bounds 28-84, autoGenerated=false)"), 12 s after the jaw part save. FIFTH wipe of this profile. Pre-wipe snapshot existed: data/calibration-backups/calibration_profiles-2026-09-25T20-19-45-314Z.json.
- Eye 2:3 placeholder deleted/recreated 21:23 UTC (never calibrated). Neck 2:2 intact: 48–180, capability.invert true, center preset 132 (measured 2026-08-23 — predates the channel change; not re-verified).
- jawAnimation.enabled was false in super-powers.json (jaw tuning sensitivity 4.2 / custom preset intact).
- Node rebooted 21:24 UTC (uptime 9 min at session start). HEAD a4f4d803, origin/main 3 commits ahead (lock-refusal status fixes). Orlok's git HEAD is OLDER than origin (1ac24bdf); its working tree matches origin/main code.
- Stall guard had released old ch4/ch8/ch11 and ch15 ("no part is mapped") after the remap — ch15 had been driven with a 1962.9 µs servo pulse by someone (the calibration page's PCA Test button, most likely) before it was mapped.

## Changes made (all through the live API unless noted; safety snapshot: /home/remote/monsterbox-safety-2026-09-25/, git ref refs/safety/pre-mina-2026-09-25 = ad916561)
1. Code sync: `git reset --mixed origin/main` (HEAD d359a58c) + routes/api/partsApi.js, routes/setup/calibration.js, config/character-locks.json written from origin/main (old copies in the safety dir). Working tree code == origin/main. Service restarted 21:42:59 UTC (after items 2–4 below, which the old process served), healthy in 4 s.
2. Jaw calibration restored: POST /api/calibration/1/profile?characterId=2 with the 20:19:45 pre-wipe body → bounds 28–84, autoGenerated false, calibrated true. Survived the restart.
3. Part 10 (Servo Channel Laser): PUT /api/parts/10?characterId=2 → pin null, config.channel 15, description updated. Survived the restart.
4. Jaw animation re-enabled: POST /setup/jaw-animation/api/jaw-animation/2 with the existing active config + enabled:true (tuning preserved: sens 4.2, 22–91, lead 500 ms).
5. Camera mic ALSA capture gain temporarily 320→160 for the witness; restored to 320 at the end.

## Hardware proof — what each witness said
Register witness = python_wrappers/i2c_servo_sampler.py reading LEDn registers on the commanded channel DURING the command (20 samples/s). Acoustic witness = camera mic (arecord plughw:4,0, 48 kHz) + XVF3800 (PyAudio via microphone_cli.py record_wav), band 1–8 kHz, baseline vs active window inside one recording.

| Part | Path used | Register witness | Acoustic witness |
|---|---|---|---|
| Jaw (1) ch11 | /api/calibration/1/goto 40↔70 ×4 | ch11 39.6→69.66°, 9 transitions (twice) | inconclusive |
| Neck (2) ch7 | goto 120↔144 ×4 (inverted → 108↔84) | ch7 59.95/83.54→107.6°, 9 transitions; API reports "Moved to 108° (commanded 120° — inverted servo)" | inconclusive |
| Eye (3) ch3 | goto 85↔100 ×4 | ch3 84.93→99.73°, 9 transitions | inconclusive |
| Poses | /poses/api/poses/1/execute (Rest 32/114) and /6 (Startled 78/168) | ch11 31.73 & 77.99; ch7 113.61 & 59.95 (=168 inverted) via daemon batch | — |
| Scene 1 Coffin Awakening | /scenes/api/1/play (real hardware) | door extend 8.5 s@100% (wrapper: GPIO 5/13, 8627 ms), sayThis FAILED (ElevenLabs quota_exceeded, scene continued), jaw 90→clamped 84 (ch11 83.54), neck 90→138 inverted (ch7 137.66), door retract 8.5 s. Scene 17.9 s. | inconclusive |
| Coffin door (4) | jog-raw extend 1.0 s / retract 1.2 s @60% (×2) | wrapper drives GPIO 5 (DIR) / 13 (PWM); pins proven with pigpio 2026-09-12/21 | inconclusive |
| LED/laser (10) ch15 | /api/parts/10/test on/off | get_duty_pca 15 → 100% (full-on) then 0%; reconcile maps ch15→part 10, so the stall guard will NOT release it | no witness possible (no camera view of the rig) |
| Burning Rose (5) GPIO16 | /api/parts/5/test on/off | pinctrl 16: hi then lo | operator-confirmed working |
| PIR (9) GPIO26 | pinctrl ×3 + /conversation/api/motion-sensor | input, lo (no motion); hasSensor true | cannot trigger remotely |
| Webcam (7) | /api/orchestration/animatronic/2/webcam-snapshot | HTTP 200, image/jpeg, 30 084 bytes | — |
| Speaker/TTS | /conversation/api/say | FAILS: "quota_exceeded … 0 credits remaining" (ElevenLabs character quota exhausted; every sayThis step fails until reset) | — |
| Microphone (8) XVF3800 | microphone_cli.py record_wav (PyAudio) | continuous non-zero frames, −24…−34 dBFS room tone | — |

Acoustic verdict: INCONCLUSIVE tonight. Idle controls swung ±2…4.5 dB (band) and room tone sat at −21…−34 dBFS and drifted (non-stationary broadband noise, peaks 70–220 Hz plus 4–8 kHz), roughly 20–28 dB above the August floor (−49 dBFS, ±0.7 dB). A servo that read +14 dB in August cannot rise above that. The camera mic delivers continuous frames only at capture gain ≥160 (at 0 it returned zeros after 0.5 s).

## Final parked state
Rest pose (jaw 32°, neck 114°), eye 92°, door tracker p=0 homed, LED off, rose off.

## Corrections from the adversarial review (same evening)
- Register readback proves the chip was commanded on the right channel, not that the part moved; motion is the operator's confirmation.
- The jaw profile's `lastCalibratedAt` now reads 2026-09-25 (the route stamps any bounds change); the true measurement date 2026-08-19 lives in `restoredFrom`.
- Servo trackers in data/actuator-positions.json are only written by the calibration goto route; the "final parked state" above comes from the chip (reconcile), not the tracker. The door was re-homed at 22:12 UTC so its p=0 is a post-move fact.
- `PUT /api/parts/:id` and the calibration profile routes ignore `?characterId` and used this node's selectedCharacter (2).
- super-powers.json config-1 min/max set to 28/84 so a future wipe falls back to the measured window; scene 1's jaw step written as 84 (it was clamped from 90 to 84 anyway).

