# Archived slash commands

Kept for reference, not loaded as commands.

- `build-v9-orlok.md`, `finish-ai-voices.md` — mission commands scoped to the v9.0/v9.1
  milestones. Both shipped; the repo is past v10.4.0 and heading for v11, and their
  step lists reference a mission doc and a state of the tree that no longer exist.
- `learn-monsterbox.md` — "read everything up front" onboarding. Superseded by the
  progressive-disclosure layout: a lean CLAUDE.md plus topic skills that load when their
  topic actually comes up. Reading the whole repo into context before knowing the task is
  the specific pattern the Claude 5 context-engineering guidance names as counterproductive.

Restore any of these with `git checkout` on an older revision, or just move the file back
up one directory.
