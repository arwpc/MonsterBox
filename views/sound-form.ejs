<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%- title %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <h1><%- title %> for <%- character.char_name %></h1>
    <form action="<%- action %>?characterId=<%- character.id %>" method="POST" enctype="multipart/form-data">
        <% if (sound && sound.id) { %>
            <input type="hidden" name="id" value="<%- sound.id %>">
        <% } %>
        <input type="hidden" name="characterId" value="<%- character.id %>">
        
        <div>
            <label for="character_id">Character:</label>
            <select id="character_id" name="characterId" required disabled>
                <option value="<%- character.id %>" selected><%- character.char_name %></option>
            </select>
        </div>

        <div>
            <label for="name">Sound Name:</label>
            <input type="text" id="name" name="name" value="<%- sound ? sound.name : '' %>" required>
        </div>

        <div>
            <label for="sound_files">Sound File(s):</label>
            <input type="file" id="sound_files" name="sound_files" accept=".mp3,.wav" multiple>
            <% if (sound && sound.filename) { %>
                <p>Current file: <%- sound.filename %></p>
            <% } %>
        </div>

        <button type="submit">Save Sound(s)</button>
    </form>

    <a href="/sounds?characterId=<%- character.id %>" class="button">Back to Sounds</a>

    <script>
        const characterId = '<%- character.id %>';
    </script>
</body>
</html>
