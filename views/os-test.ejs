<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <h1><%= title %></h1>
    <div class="os-info">
        <h2>Operating System Information</h2>
        <ul>
            <li><strong>Platform:</strong> <%= osInfo.platform %></li>
            <li><strong>Release:</strong> <%= osInfo.release %></li>
            <li><strong>Type:</strong> <%= osInfo.type %></li>
            <li><strong>Architecture:</strong> <%= osInfo.arch %></li>
            <li><strong>Total Memory:</strong> <%= (osInfo.totalMemory / (1024 * 1024 * 1024)).toFixed(2) %> GB</li>
            <li><strong>Free Memory:</strong> <%= (osInfo.freeMemory / (1024 * 1024 * 1024)).toFixed(2) %> GB</li>
        </ul>

        <h3>CPU Information</h3>
        <% osInfo.cpus.forEach((cpu, index) => { %>
            <div class="cpu-info">
                <h4>CPU <%= index + 1 %></h4>
                <ul>
                    <li><strong>Model:</strong> <%= cpu.model %></li>
                    <li><strong>Speed:</strong> <%= cpu.speed %> MHz</li>
                </ul>
            </div>
        <% }); %>
    </div>
</body>
</html>
