<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>System Config</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        table {
            border-collapse: collapse;
            width: 100%;
            margin-bottom: 20px;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
        }
    </style>
</head>
<body>
    <h1>System Config</h1>
    <h2>System Information</h2>
    <ul>
        <li>Platform: <%= systemInfo.platform %></li>
        <li>Architecture: <%= systemInfo.arch %></li>
        <li>Hostname: <%= systemInfo.hostname %></li>
        <li>Total Memory: <%= systemInfo.totalMem %></li>
        <li>Free Memory: <%= systemInfo.freeMem %></li>
        <li>CPUs: <%= systemInfo.cpus %></li>
        <li>Uptime: <%= systemInfo.uptime %></li>
    </ul>
    <h2>Network Information</h2>
    <ul>
        <li>IP Address: <%= ipAddress %></li>
        <li>Wi-Fi Signal Strength: <%= wifiSignal %></li>
    </ul>
    <h2>Power Information</h2>
    <ul>
        <li>Current Power: <%= power %></li>
    </ul>
    <h2>Drive Space Information</h2>
    <% if (driveInfo.length > 0 && !driveInfo[0].error) { %>
        <table>
            <tr>
                <th>Filesystem</th>
                <th>Size</th>
                <th>Used</th>
                <th>Available</th>
                <th>Mount Point</th>
            </tr>
            <% driveInfo.forEach(function(drive) { %>
                <tr>
                    <td><%= drive.filesystem %></td>
                    <td><%= drive.size %></td>
                    <td><%= drive.used %></td>
                    <td><%= drive.available %></td>
                    <td><%= drive.mountpoint %></td>
                </tr>
            <% }); %>
        </table>
    <% } else { %>
        <p>Unable to retrieve drive information.</p>
    <% } %>
    <a href="/">Back to Main Menu</a>
</body>
</html>