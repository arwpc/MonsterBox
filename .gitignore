repopack-output.txt
node_modules/
.idea/
# Ignore all log files, including those in the /log directory
*.log
MonsterBox*.log
/log/
.env
*.env
certs/

# Added by Task Master AI
# Logs
logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
dev-debug.log
# Dependency directories
# Environment variables
# Editor directories and files
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# OS specific
.DS_Store
# Python cache files
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
# Task files (but not .vscode/tasks.json)
/tasks.json
tasks/
# MkDocs generated site
site/
mkdocs-site/
data/last-used-character.json
data/port-registry.json
data/last-used-character.json
data/port-registry.json


# Playwright/UI test artifacts (large files) - ignore
playwright-report/
test-results/
tests/playwright-report/
tests/test-results/
.playwright/

# App-scoped Playwright artifacts
apps/monsterbox4/playwright-report/
apps/monsterbox4/test-results/
apps/monsterbox4/.playwright/

# Optional artifact subfolders (if configured)
apps/monsterbox4/tests/ui/screenshots/
apps/monsterbox4/tests/ui/videos/
apps/monsterbox4/tests/ui/traces/


# Generic archives
*.zip
.augment/
.cursor/

# Claude Code — ignore session data, keep commands/agents/skills/settings
.claude/*
!.claude/commands/
!.claude/agents/
!.claude/skills/
!.claude/settings.json

# Runtime state (regenerates every run, per-node — not source-of-truth)
data/monsterbox.pid
data/movement-telemetry.json
data/scene-analytics.json
data/performance-history.json
data/startup-health.json
data/actuator-positions.json
data/character-*/lurk-mode-state.json
# Per-node calibration — globally keyed by partId but partIds are per-character,
# so syncing across nodes causes adapter/type mismatches (Orlok actuator → Mina servo).
data/calibration_profiles.json
