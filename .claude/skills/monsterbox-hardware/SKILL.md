---
name: monsterbox-hardware
description: Commanding MonsterBox physical hardware — the physical-risk parts and the incident behind each (per-part software safety was removed entirely at operator direction 2026-08-20; knowledge, not enforcement), the servo-kind traps (continuous vs absolute vs multi-turn), what calibration data may be trusted, and how to prove a part is alive when every success field can lie. Load before commanding, calibrating, probing, or re-typing any part.
---

# MonsterBox hardware

## Physical-risk parts — software refuses NOTHING; the knowledge still applies

**2026-08-20 operator ruling: every per-part safety limit was removed, all characters,
permanently** ("we don't need them ever again"). `config/hardware-safety.json` is empty and
must stay that way — do not re-add blocks, windows, caps, exclusions, or serialization
without Aaron's direction. He manages physical risk at the rig. The incidents below remain
true as *knowledge*: an agent drives these parts only on operator direction, never on its
own initiative, and no automated suite should command them.

1. **Orlok (3) parts 3, 4, 5.** Bow (3): wiring docs contradict — "extend" may physically
   retract, and it rests at its mechanical minimum (retraction bends the linkage). Elbow (4):
   electrically dead. Forearm (5): travel never measured. Parts 4+5 share a rail that has
   **blown fuses under simultaneous load**; dedicated 10 A circuits are on Aaron's list.
2. **Sir Dragomir (4) part 1 (neck).** goBILDA Stingray-2, 900° multi-turn — a full rotation
   **tears the head cabling**. Uncalibrated. Move it on operator direction, eyes on it.
   **FINAL channel map (operator ruling 2026-08-22, "for once and always"): jaw=3, head=7,
   box=11** — `docs/hardware/PCA9685-CHANNEL-MAP-DRAGOMIR.md`. Three older maps circulated the
   same day and each stale assertion caused a channel collision; trust only that file.
3. **Do not relabel Orlok part 15 ("Head on a Swivel") as multi-turn.** Probably also a
   Stingray, but Aaron's measured 0–180-scale windows work; remapping 0–180→0–1800 silently
   breaks a working head. Mirror-image of the knight: there the wrong label caused harm,
   here changing it would.

## Servo-kind traps

- `config.servoType: "continuous"` turns an ANGLE command into a **timed spin**
  (`rotate_continuous_pca <ch> <dir> <speed> 1000`) and discards the position. Check the
  kind before sending any angle — this accumulated 99 spins on the knight's neck.
- `minPulse`/`maxPulse`/`neutralPulse` in `servo_models.json` are **dead data** — pulse
  mapping is hardcoded (standard 500–2400 µs, continuous 1000/1500/2000). Measured: 131°
  → 1880 µs. Don't chase them.
- Inverted parts historically reported the angle *asked for*, not *driven* — verify with the
  register sampler when it matters.

## Calibration data

- Read bounds via `calibratedBounds(profile)` / `store.get()` — never raw `profile.bounds`.
  `autoGenerated: true` spans are placeholders the read path deliberately withholds.
  **A pose may only use a part with a real measured window.**
- Trusted windows live in `docs/development/V11-HANDOFF.md` §3, read from each node's own
  files (node-local — see `monsterbox-fleet`).
- Every `controlPart()`/calibration-store call must pass `characterId` — part ids are only
  unique within a character; omitting it has driven the wrong PCA9685 channel.
- `nudge` now seeds from the position store and **refuses when position is unknown** — that
  refusal replaced a 43° jump into an endstop. Don't weaken it.
- **A PCA9685 channel holds its last pulse: plugging a servo lead into a live channel slams
  the servo to that stale position and holds it powered** (the knight's head snapped to an
  unsafe spot the instant it was reconnected, 2026-08-22). Release the channel first —
  `POST /api/calibration/<partId>/release` or the calibration page's Release button — then
  plug/unplug; the next move re-energizes it.

## Proving a part is alive

- **A PCA9685 cannot report what is attached** — one-way I²C→PWM, servos have no
  back-channel. Driving and observing is the only method. USB devices are the opposite:
  identify them by `idVendor:idProduct` (model-level, never per-unit serial).
- `services/hardwareService/exec.js` returns `status:'success'` with **`simulated: true`**
  on fallback, logging the reason only to `/var/log/monsterbox.err`. **Reject
  `simulated:true`**; score aliveness on driver reach or observed motion, never a success field.
- Acoustic witnesses trap: a dead capture device or ALSA `default` routed to one scores
  everything silent. Run a **positive control and an idle control** before any silence means
  anything. Per-channel measured map for Mina: `docs/hardware/PCA9685-CHANNEL-MAP-MINA.md`.
- ReSpeaker XVF3800: `arecord`/`parec`/`ffmpeg` open it and deliver zero frames — PyAudio via
  `python_wrappers/microphone_cli.py` is the only proven capture path. Judge on FRAMES
  (non-zero bytes AND RMS). Traps: `docs/hardware/RESPEAKER-XVF3800.md`.

Hands-only work lives in `docs/hardware/OPERATOR-TODO.md` — add to it; don't attempt it in software.
